<%= form_for(user1) do |f| %>
  <% if user1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user1.errors.count, "error") %> prohibited this user1 from being saved:</h2>

      <ul>
      <% user1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="w3-panel">
    <%= f.label :name %>
    <%= f.text_field :name , class:"w3-input" %>
  </div>

  <div class="w3-panel">
    <%= f.label :email %>
    <%= f.text_field :email , class:"w3-input" %>
  </div>

  <div class="w3-button">
    <%= f.submit :Submit, class:"w3-button w3-red" %>
  </div>
<% end %>
